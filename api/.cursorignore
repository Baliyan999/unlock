# Cursor ignore file
# This file tells Cursor which files to ignore

# We want to allow .env files to be visible in Cursor
# even though they are in .gitignore for security

# Uncomment the line below to allow .env files:
# .env

# Or we can explicitly ignore other files:
node_modules/
dist/
*.log
